# Tasks

- [ ] Provide `projects` and a matching `currentProject` in the fixture context.
- [ ] Give one fixture task a parent and assert the single epic bar row.
- [ ] Run the backlog batch browser test and every other browser test; compare
      with the baseline.
- [ ] Run `npm test` in `web/`.
