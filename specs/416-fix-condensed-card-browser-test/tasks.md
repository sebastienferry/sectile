# Tasks

- [ ] Rewrite `useApp` for every module under `/src/components/`.
- [ ] Add the context fields the card's children read.
- [ ] Run the condensed card browser test and every other browser test.
- [ ] Run `npm test` in `web/`.
