# Tasks

- [ ] Add `web/tests/browserRoot.mjs` and its unit test.
- [ ] Use the helper in the ten browser tests; pass `resolve.preserveSymlinks`.
- [ ] Name an absolute `PLAYWRIGHT_MODULE` in each run comment; add the README
      section.
- [ ] Run every browser test from a path without `#` and from a copy under `#`.
- [ ] Run `npm test` and `npm run lint` in `web/`.
