{
  "name": "taskflow-desktop",
  "version": "0.2.0",
  "private": true,
  "main": "electron/main.cjs",
  "scripts": {
    "build": "vite build",
    "start": "electron .",
    "package": "electron-packager . TaskFlow --out=release --overwrite --extra-resource=bin/taskflow --ignore='^/(bin|tests|release[^/]*)(/|$)'",
    "test:ui": "node --test --test-concurrency=1 tests/*.ui.cjs"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@electron/packager": "^20.3.0",
    "@playwright/test": "^1.63.0",
    "electron": "^44.3.0",
    "vite": "^8.3.0"
  }
}
