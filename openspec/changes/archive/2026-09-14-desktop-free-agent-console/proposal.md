# Free interactive agent consoles

Allow desktop users to open Codex or Claude in a project's mapped repository without selecting a task, skill, or initial prompt. Each console is a separate local execution with its own PTY, supervised process, and replay history.

The project action opens an agent selector. Launch invokes only the selected executable with no arguments. Workflow command templates and task instructions do not apply. No task or tracker activity is created or updated.
