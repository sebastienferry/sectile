/**
 * Strings of the project settings (#528): every tab of the project modal,
 * provider descriptions and the board column editor.
 *
 * French is the reference and keeps the wording the interface already had;
 * English is typed after it, so a key missing in English fails the build.
 */
const fr = {
}

export type ProjectSettingsStrings = typeof fr

const en: ProjectSettingsStrings = {
}

export const projectSettings = { fr, en }
