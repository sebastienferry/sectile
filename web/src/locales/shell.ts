/**
 * Strings of the web shell and the board (#526): sidebar, project picker,
 * header, pinned bar, board and backlog, cards, filters, command palette,
 * display scale menu and status bar.
 *
 * French is the reference and keeps the wording the interface already had;
 * English is typed after it, so a key missing in English fails the build.
 */
const fr = {
}

export type ShellStrings = typeof fr

const en: ShellStrings = {
}

export const shell = { fr, en }
