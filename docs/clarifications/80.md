# Clarification — GitHub #80

## Identity and status

- Task: `gh-9dc93da8-0cb4-4fd5-af38-663433d960ff-80`.
- Issue: https://github.com/sebastienferry/sectile/issues/80 — Remove project from desktop app.
- Project: `9dc93da8-0cb4-4fd5-af38-663433d960ff`, repository `sebastienferry/sectile`.
- Assigned branch: `feat/80`; existing workspace changes preserved.
- Invocation: `f095f256-465e-49a3-b283-76e52e39ccdf`, standalone remote execution using MCP stage reporting.
- Live ticket has no description or comments; no parent context was returned.
- Status: both essential product decisions are resolved by the user; ready for specification.

## Restated request and scope

Allow users to disconnect a project from this workstation through the desktop application, removing its local mapping and per-project overrides and preventing new executions until it is explicitly added again. Preserve server projects, tracker tasks, repository files, worktrees, and deployed tooling; server-wide deletion and disk cleanup are outside this ticket.

## Evidence and files read

- `desktop/src/main.js`: sidebar combines projects with resolved paths and projects referenced by any execution; Add project also considers execution history when deciding whether a project is already added. Local settings offer mapping and overrides but no removal. Task archive hides existing runs in local storage, requires confirmed stop for active runs, and preserves server tasks.
- `desktop/electron/main.cjs` and `desktop/electron/preload.cjs`: IPC exposes project discovery, mapping, configuration, stop, detach, and global history clearing, but no project removal.
- `cmd/server/agent_desktop.go`: `/desktop/projects` accepts GET and POST only. Mapping updates use the preparation mutex and persistent settings. Global history cleanup removes only executions whose process exit has been confirmed; it is not suitable for removing one project.
- `cmd/server/agent_config.go`: repository resolution can fall back to the agent's project identity or matching Git remote when no explicit mapping exists. Deleting a map entry alone does not reliably disconnect a project.
- `internal/agentconfig/local.go` and `internal/agentconfig/settings.go`: workstation mappings and per-project overrides are distinct from repository configuration. Shared settings writes preserve connection fields and use atomic replacement. Missing settings fields can fall back to legacy repository settings.
- `web/src/components/ProjectModal.tsx`, `web/src/context/AppContext.tsx`, `internal/handlers/handlers.go`, and `internal/db/db.go`: web deletion invokes server project deletion, reassigning tasks to the default project. This is a different scope from the proposed desktop-local removal.
- `desktop/README.md`: documents independent desktop/agent lifetimes, persistent workstation settings, in-memory console history, and local archive behavior.
- `desktop/tests/console.ui.cjs`: inspected relevant project discovery and archive test excerpts; existing UI harness can cover removal behavior later.
- `AGENTS.md`, `.taskflow/config.json`, and `docs/clarifications/48.md`: workflow, project identity, and report convention. No project MEMORY file was found.

## Resolved decisions

1. **Disconnect from the workstation.** The user answered “Disconnect.” Remove the local mapping and per-project overrides, hide the project's consoles, and require explicit re-add before new executions. Automatic repository matching and legacy settings must not silently reconnect it.
2. **Block removal during active executions.** The user accepted the recommendation to block removal while executions are queued, preparing, or running. Explain that executions must first be stopped; do not implicitly cancel them. Removal must wait for confirmed process exit where applicable.

No essential questions remain.

## Settled scope and explicit assumptions

- This invocation produces clarification only; no production code or specification is written.
- Interpret “from desktop app” as a workstation-local action; do not reuse server project deletion.
- Reversible UI assumption: place Remove from desktop in the existing Local project settings, with confirmation describing its effects and a visible error on failure.
- Preserve finished console records rather than erasing logs as an incidental consequence; hide them while the project is removed and allow them to be available again after re-add during the agent lifetime. This is an explicit assumption consistent with local archive behavior; no new durable log storage is required.
- Successful removal should survive desktop/agent restart, allow re-add through the existing discovery flow, and clear stale selected-project/terminal UI state. Disconnection must also prevent implicit repository resolution until explicit re-add.
- Keep changes scoped to the selected project and preserve unrelated mappings, connection settings, repository content, and server/tracker state.

## Critical dependencies and specification follow-up

- Both product answers have been received; no outstanding dependency blocks specification.
- For disconnection, coordinate authenticated local-agent operations, Electron IPC, settings persistence, and execution admission so an incoming launch cannot race removal. Account for queued/preparing/running states and confirmed process exit.
- Explicitly cover implicit repository matching, legacy configuration fallback, existing console groups, and Add project's Already added calculation; otherwise removed projects can reappear or cannot be re-added.
- No external team, credentials, new service, or server database migration is currently identified as necessary. Any new local settings field and its compatibility behavior belong in the later specification.
- Validate later with focused agent/settings tests and the existing desktop UI harness, covering cancellation, active work, persistence/restart, re-add, failure handling, and preservation of unrelated data.

## Validation and workflow outcome

Code inspection only; no tests were run because implementation did not change. Live MCP reads verified task/project identity, and the supplied remote run ID was reused. The user supplied both decisions after the initial run ended. The original run cannot be reopened (Sectile returned “remote run does not match an active execution on this task”), so a continuation run records finalization. Sectile accepted the clarified transition on branch `feat/80`, returned status `to_specify` and label `#clarified`, and queued tracker synchronization. Continuation run: `8469fe74-c0ae-4ba2-962b-8603bd290ee1`. Specification and implementation remain outside this invocation.
