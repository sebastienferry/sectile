# Clarification — GitHub #377

Task: `gh-ef5a2777-920f-4744-a7e8-a58a4c257a23-377`
URL: https://github.com/sebastienferry/sectile/issues/377
Branch: `feat/377`

## Round 1 — analysis (2026-09-23)

Restore the spinning running icon in the desktop sidebar and discussion header.
Changing run lifecycle semantics, notification icons, or the web interface is out of scope.

### Evidence and files read

- `shared/runStates.ts`: the running glyph has `spins: true`; SVG serialization intentionally produces static markup for notifications too.
- `desktop/src/main.js`: sidebar and header place that SVG inside `.run-state` with the resolved state in `data-run-state`. Existing rendering preserves it when the state does not change.
- `desktop/src/style.css`: styles the SVG but supplies no rotation animation.
- `desktop/tests/discussion-header.ui.cjs` and `desktop/tests/waiting-notification.ui.cjs`: exercise the real Electron renderer and state transitions, but do not check animation.
- `desktop/package.json`: defines build, unit tests, and Electron UI tests.
- `AGENTS.md` and `.agents/MEMORY.md`: repository conventions and architecture constraints reviewed.

### Settled decisions

Animate only the resolved running state on both desktop surfaces, with a continuous one-second linear rotation. Keep the accompanying label stationary. Waiting, queued, preparing, completed, failed, and canceled states remain static. Respect the system reduced-motion preference by disabling the rotation. Preserve shared glyphs, colors, labels, notifications, and existing polling behavior.

These are reversible presentation choices consistent with the shared state definition. No service, migration, new dependency, or product decision is needed. Validate actual animation in Electron, including polling, transitions, and reduced motion.

### Status

Round 1 complete. Zero open product questions; scope is settled and the clarification exit condition is met.
