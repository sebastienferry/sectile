# Clarification — GitHub #349

Task: `gh-11a4f59c-b6bd-4747-8ea5-0a10d5b78da4-349`
URL: https://github.com/sebastienferry/sectile/issues/349
Branch: `feat/349`
Type: Feature — "for Jira tracker add an option to filter issues pertaining to a project"

## Round 1 — analysis (2026-09-22)

### Restated request and scope

A Jira project key is today the whole of what a Sectile project imports: every work item
of `PE` of the configured issue types lands on the board. This ticket adds a per-project
option that narrows that import to the work items carrying a given Jira **label**, so
several Sectile projects can live on one Jira project key, each owning its own slice.
Out of scope: any equivalent for GitHub or GitLab, changing what a Jira label means for
the workflow stage (ADR 0013 keeps its meaning), the board/column mapping, and removing
from the local board the tasks that were imported before the filter existed (see
ambiguity 4 — it is a real consequence, not a silent one).

### Files read

- `internal/trackerapi/jira.go` — `SyncIssues` (l. 152), `search` (l. 116), `GetIssue`
  (l. 167), `CreateIssue` (l. 194), `UpdateIssue` / `labelOps` (l. 329, 391),
  `UpdateLabels` (l. 709), `GetEpics` (l. 977), `cleanLabels` (l. 314)
- `internal/trackerapi/jira_mapping.go` — `jiraJQL` (l. 250), `cleanJiraKey`, `jiraTask`
- `internal/tracker/ticketing.go` — `SyncRequest` (l. 131), `CreateIssueRequest` (l. 85)
- `internal/models/models.go` — `Project` (`JiraProject`, `IssueTypes`),
  `CreateProjectRequest`, `UpdateProjectRequest`
- `internal/db/db.go` — schema and migration of `projects.jira_project` (l. 346, 409,
  730), project read/insert/update (l. 5710, 6029, 6254), `ImportOrUpdateTasks` (l. 939),
  `processSyncJob` (l. 4083), `afterTrackerSync` (l. 3931), `CreateTask` remote creation
  (l. 2308), `PushTaskToTracker` (l. 5377), `workflowLabelVariants` /
  `StaleWorkflowLabels` (l. 2150, 2160), `jiraProjectKeyFor` (l. 5502)
- `internal/db/pins.go` — the `pinned` label convention
- `internal/agentconfig/config.go` — what the agent context exposes of a project
- `web/src/components/ProjectModal.tsx` — the Jira block (l. 1205-1280)
- `docs/adrs/0013-labels-carry-the-stage-on-jira.md`

### Verified findings

1. **There is a clean insertion point.** `jiraJQL(projectKey, issueTypes, extra)`
   (`jira_mapping.go:250`) already appends an arbitrary `AND <extra>` clause; `SyncIssues`
   passes `""` today and `GetEpics` passes `issuetype = Epic`. A label filter is one more
   `extra` term, `labels = "<value>"`, and needs no new query plumbing.

2. **Nothing Sectile writes would strip the label.** Jira label writes go through
   `labelOps` (`jira.go:391`), which emits `add` / `remove` operations rather than
   replacing the array, and `StaleWorkflowLabels` only ever targets the stage spellings
   listed in `workflowLabelVariants`. A project label set by a human survives every stage
   advance.

3. **Jira labels cannot contain spaces and are case-sensitive.** `cleanLabels`
   (`jira.go:314`) already rewrites spaces to `-` on the write side; JQL `labels = "x"`
   does not match `X`. Whatever the option stores has to be matched verbatim, and a value
   with a space has to be rejected or rewritten at input time rather than silently
   producing a filter that matches nothing.

4. **The import never prunes.** `ImportOrUpdateTasks` (`db.go:939`) is an upsert over the
   returned work items, and `afterTrackerSync` only refreshes teams, columns and PR links.
   A work item that stops matching the filter is not removed from the local board; it
   simply stops being refreshed. This is a behaviour of the feature, not a bug to fix here
   — but it decides what "turning the option on" means for an existing project (question 4).

5. **The setting belongs on the project, not on Settings.** `JiraProject` has a
   user-level fallback (`db.go:2241`, `4701`), but a label whose purpose is to separate two
   Sectile projects sharing one key is meaningless at user level: a global value would
   apply the same slice to both. `IssueTypes` is the right precedent — project-only.

6. **Creation is the hole to watch.** Both remote-creation paths (`db.go:2308` and
   `db.go:5377`) pass only the task's own labels. With a filter on and nothing else done,
   a work item Sectile creates would be written to Jira without the project label and
   would disappear from the board at the next sync (question 3).

### Resolved technical choices (no answer needed)

- **Storage.** One new column `projects.jira_label` (TEXT NOT NULL DEFAULT ''), added with
  the same idempotent `ALTER TABLE ... ADD COLUMN` pattern as `jira_project`
  (`db.go:730`), plus the field on `Project`, `CreateProjectRequest`,
  `UpdateProjectRequest` and the three SQL statements that name the project columns.
- **Query.** `SyncIssues` builds the `extra` clause; the value is quoted and its `"`
  escaped exactly as `jiraJQL` already does for the project key.
- **Empty means today's behaviour.** No label configured ⇒ no `labels` term in the JQL, so
  every existing project keeps importing exactly what it imports now.
- **UI.** A text input in the `issueTracker === 'jira'` block of `ProjectModal.tsx`, under
  "Projet Jira (clé)", French copy like the rest of that panel.
- **Not exposed to agents.** `internal/agentconfig` publishes the tracker identity an
  agent needs (`jiraProject`, repo, remote); a sync-side filter is not part of that
  contract and stays out of `AGENTS.md`.
- **Tests.** `internal/trackerapi/jira_test.go` covers the adapter with a stub site and is
  where the JQL assertion goes; `internal/db/jiratracker_test.go` covers the project-level
  plumbing.

### Dependencies

- SQLite migration on the `projects` table — additive, and the existing pattern makes it
  safe on an already-populated database.
- No third party, no other service, no data to backfill. The labels themselves have to
  exist in the customer's Jira, which is a setup step for whoever turns the option on, not
  work for this ticket.

## Open product questions (Round 1)

1. **Who spells the label?** Free text typed per project (e.g. `team-alpha`, an existing
   label of the customer's Jira), or a value Sectile derives from the project
   (`sectile-<slug>`) and owns? — *Recommendation: free text.* The labels usually already
   exist on the board; imposing a spelling would force a mass re-labelling in Jira and
   would break the moment a project is renamed.

2. **One label, or several?** A single value, or a list matching `labels IN (a, b)`? —
   *Recommendation: a single value for now.* No use case in the ticket needs more, and a
   list can be added later without changing what is stored first.

3. **Should Sectile stamp the label on the work items it creates?** With the filter on,
   an issue created from Sectile that does not carry the label vanishes from the board at
   the next sync. — *Recommendation: yes, add it automatically on creation* (both
   `CreateTask` and `PushTaskToTracker`), which is the only behaviour that makes the
   feature self-consistent.

4. **What happens to what is already on the board when the filter is turned on, or
   narrowed?** Today nothing: the non-matching cards stay, frozen, because the import never
   prunes (finding 4). — *Recommendation: accept it for this ticket and say so in the UI
   hint;* pruning touches every tracker and deserves its own ticket.

5. **How far does the filter reach?** `SyncIssues` only, or also `GetEpics`
   (`jira.go:977`), which lists the epics offered as a parent when creating a work item? —
   *Recommendation: both;* an epic belonging to another Sectile project has no business in
   that list. `GetIssue` by explicit key stays unfiltered either way — asking for `PE-42`
   by name is an explicit request, not a browse.

Answers expected from the work item's owner.

## Stage

Round 1 complete; the exit condition is **not** met — the five questions above are open,
so the task stays at `new`.

## Round 2 — answers from the owner (2026-09-22)

### What the owner decided

> "For 4 & 5: I want to actually sync the whole board but only display *by default* the
> tickets related to the project. Add a new quick filter to display all tickets of the
> board, and the possibility, in the burger menu of the task card, to add the ticket to
> the project — which will just add the label to the ticket."

**This reverses the shape of the feature**, and questions 4 and 5 are settled by it.

### Reversed assumptions

- **The label is no longer a sync filter.** Round 1 resolved, as a technical choice, that
  `SyncIssues` would add `AND labels = "<value>"` to its JQL. It will not: the sync keeps
  importing the whole Jira project exactly as it does today, and `jiraJQL`'s `extra`
  clause stays unused by the sync. Question 5 ("how far does the filter reach:
  `SyncIssues`, `GetEpics`?") therefore has no object — nothing on the Jira side is
  narrowed, epics included.
- **Question 4 dissolves.** The board never loses a card, so there is nothing stale to
  prune: a ticket that does not carry the label is imported, stored and simply not shown
  by default. Turning the option on or changing its value is instantaneous and reversible,
  which the sync-side filter would not have been.
- **The label becomes a membership mark, not a query.** Attributing a ticket to a Sectile
  project is now an explicit gesture a human makes on a card, and the label is what records
  it on the tracker so the attribution survives a resync and is visible to the team in Jira.

### Settled scope after Round 2

1. **Sync:** unchanged. The whole Jira project, filtered only by issue type as today.
2. **Default display:** in a project that has a label configured, the board, list and
   roadmap show only the tasks carrying it.
3. **Quick filter:** a toggle showing every ticket of the board, the label carriers and
   the rest.
4. **Card action:** an entry in the task card's burger menu that adds the project's label
   to the ticket, which writes it to the tracker.

### Newly resolved technical choices

- **The default filter runs server-side.** Every board filter except `source` and `parent`
  is a query parameter of `GET /api/tasks`, assembled by `buildTaskQuery`
  (`AppContext.tsx:977`) and applied by `GetTasksForUser` (`handlers.go:1479`); the facets
  and the counts come from the same call. A client-side filter would leave the counts, the
  facets and the sprint totals describing a list nobody sees. One more parameter, and the
  existing `label` filter (already exposed to the user) is left alone.
- **The quick filter is persisted like its peers.** `pinnedOnly` and `activeOnly` are
  booleans kept per project in `localStorage` under `sectile_filters_<projectId>`
  (`AppContext.tsx:618-675`). The new toggle follows that, with one difference: its
  default is "project only", so a project that configures a label sees its own slice
  without touching anything.
- **The card action needs no new endpoint.** `PATCH /api/tasks/:id` with `labels` already
  diffs against the stored set, records the removals and pushes both to the tracker
  through `enqueueTrackerUpdateAsUnsafe` (`db.go:2555-2566`, `db.go:2730`); the Jira
  adapter turns that into `add` / `remove` operations (`jira.go:391`), so nothing else on
  the ticket is touched. The menu entry is `updateTask(id, { labels: [...labels, projectLabel] })`.
- **The entry only appears when there is something to do:** a project with a label
  configured, on a ticket that does not already carry it.
- **Views follow for free.** Board, list, roadmap and sprint views all read the same
  filtered task list from the context, so none of them needs its own rule.

### Consequence worth stating (question 9 below)

The whole board is now imported *per Sectile project*. Tasks are keyed by
`project_id` + `key` (`db.go:964`), so two Sectile projects sharing the Jira key `PE` each
hold a full copy of `PE` locally, and "show all tickets of the board" shows the same Jira
tickets in both. That is the direct price of syncing everything, and it is bounded by the
size of the Jira project — but it is a change in local volume worth acknowledging before
implementation rather than discovering on a 3000-ticket board.

## Open questions after Round 2

Still unanswered from Round 1:

1. **Who spells the label?** Free text typed per project, or a value Sectile derives
   (`sectile-<slug>`)? — *Recommendation: free text.*
2. **One label, or several?** — *Recommendation: a single value.*
3. **Does a work item Sectile creates get the label automatically?** This matters more
   than it did in Round 1: without it, a ticket created from Sectile in a labelled project
   would not appear in that project's own default view, and the user would have to add it
   to the project by hand right after creating it there. — *Recommendation: yes, stamped
   on creation.*

New:

6. **A symmetric "remove from the project"?** The menu would drop the label from a ticket
   that carries it. — *Recommendation: yes;* otherwise a mistagged ticket has to be fixed
   in Jira.
7. **Jira only, or every tracker?** The mechanism — a project label, a default filter, a
   card action — is tracker-agnostic, and GitHub labels are already written the same way.
   Restricting it to Jira is a UI decision, and it decides the naming: `jiraLabel` if Jira
   only, `projectLabel` if not. — *Recommendation: build it tracker-agnostically, surface
   it wherever a project label is configured.*
8. **What does the filter do with the tickets that carry no label at all?** They are
   hidden by default, and reachable only through the "show all" toggle, which is where the
   card action is used. — *Recommendation: confirm that reading;* it is the only one that
   makes the toggle meaningful, but it means a fresh project with a label configured and
   nothing tagged yet opens on an empty board.
9. **Is the duplicated local import acceptable?** (see the consequence above) —
   *Recommendation: yes, accept it;* the alternative is the sync-side filter that was just
   rejected.

Answers expected from the work item's owner.

## Stage

Round 2 complete; the exit condition is **not** met — six questions are open, so the task
stays at `new`.
