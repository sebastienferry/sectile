# Clarification #426 - Port the remaining taskativ macro-workflow features

- Ticket: https://github.com/sebastienferry/sectile/issues/426
- Parent: M-7 "Ux improvements and fixes"
- Branch: `feat/426`

## Round 1 (2026-09-24)

### Restatement

Port what PR #423 left out of taskativ's macro workflow: the surgical
`realign-macro` skill, one worktree per macro, sprint management, and close
the four gaps #423 documents in place (`TargetProjectID` not consumed, one
repository path per project, one key prefix, no `scenarios` slicing source).
Out of scope: anything upstream of the slicing that #423 already ported (origin
fields, `tasks.md` / `spec.md` import, `phase:` / `goal:` axes), and a
`specify-macro` skill, which Sectile does not have and the ticket does not ask
for.

### Files read

- `taskativ/docs/MACRO_WORKFLOW.md` (the reference reading order)
- `taskativ/internal/db/sprints.go` (function list and header)
- `taskativ/internal/db/skilltemplates.go` (`realignMacroFrameworkBody`),
  `taskativ/internal/db/realignmacro_test.go` (names only)
- `internal/models/models.go` (`MacroTodo`, `MacroTodoFrom*`, `Project`,
  `UseWorktrees`, `SkillDirNames`)
- `internal/db/macros.go` (`CreateStoryFromMacroTodo`, todo normalisation)
- `internal/db/sddslicing.go` (`macroSpecRepoPath`, `FindMacroSpecDir`,
  `findMacroBranch`, `TodosFromSDD`)
- `internal/handlers/handlers.go` (macro `refine` and `slicing` routes)
- `internal/skills/catalog.go`, `internal/skills/testdata/golden/refine_macro.speckit.skill.md`
- `internal/trackerapi/jira.go` (`SetSprint`, `ListSprints`),
  `internal/db/teams.go` (`SetTasksSprint`)

### Findings from the code

- **Sprints are not "local" in taskativ.** `sprints.go` creates, updates and
  deletes sprints *on the tracker* (Jira Agile sprints, GitLab iterations),
  synchronously, and only mirrors them locally. Its header says why: a local
  sprint could never receive a ticket, since the Agile API only moves a ticket
  to a sprint id it knows. The ticket's "local sprint management" wording
  therefore reads two ways (see Q2). Sectile today only reads sprints
  (`ListSprints`) and assigns tickets to them (`SetSprint`).
- **GitHub has no sprints.** This repository's own project is on GitHub, where
  milestones are already used as macros. Sprint management can only apply to
  Jira and GitLab projects.
- **`realign-macro` writes in the macro's worktree, on its branch.** In
  taskativ the worktree-per-macro feature is what prepares that branch, "so no
  skill switches branch". The two features are coupled: realignment without a
  prepared macro branch has nowhere safe to write.
- **The worktree-per-macro feature targets the specifications repository.**
  Sectile has one `RepoPath` per project (`macroSpecRepoPath` documents it).
  A macro worktree created in the code repository is the wrong place for a
  team whose specifications live elsewhere, so the "specifications repository"
  setting is a prerequisite of the macro worktree, not an independent gap.
- **Sectile has no `specify-macro`.** Its only macro skill is `refine-macro`,
  which proposes todos and tickets but writes no specification. `realign-macro`
  assumes a specification already exists in the repository (written by hand,
  by taskativ, or by a future skill); `FindMacroSpecDir` already finds it.
- **The `scenarios` source needs the EPIC template field map**, which Sectile
  lacks entirely (read, display, edit, concurrent-edit guard). That is a
  feature in its own right, not a reader.
- **`TargetProjectID` crosses trackers.** Creating a story in another project
  means another tracker, possibly another kind: a GitHub milestone is per
  repository and cannot parent an issue in another repository, whereas a Jira
  epic can parent a story from another project of the same instance.

### Ambiguities (worst first)

1. **Delivery shape.** Eight items of very different size in one ticket: one
   PR or several tickets. Changes every downstream stage (see Q1).
2. **Sprint management target.** Tracker writes (as taskativ) versus a local
   sprint model; and which operations (create, rename, dates, close, delete).
   (Q2)
3. **Cross-project story creation.** What happens when the target project
   cannot carry the macro as parent. (Q3)
4. **`scenarios` source.** In scope with the field map, or deferred. (Q4)

### Critical dependencies

- Specifications repository setting → worktree per macro → `realign-macro`
  (writes on the macro branch). This order is forced.
- Sprint writes need Jira Agile write scopes and GitLab iteration APIs;
  tracker writes must go through the tracker operation queue except where
  taskativ deliberately calls synchronously (sprint creation).
- New project settings are new columns: numbered migrations only, never the
  frozen baseline (`internal/db/migrations.go`).
- `scenarios` source → EPIC template field map (not present).

### Reversible technical choices, resolved from conventions

- **`realign-macro` is a macro-scoped skill** registered next to
  `refine-macro` (`SkillDirNames`, `internal/skills/catalog.go`, golden files
  per SDD framework), launched from the macro panel like `refine-macro`.
  Its body ports taskativ's `realignMacroFrameworkBody` (Spec Kit and
  OpenSpec variants), with the surgical rules: add, rename, mark orphans as to
  be removed, never touch an existing entry's body, never delete.
- **Specifications repository** is a second, optional path on the project,
  falling back to `RepoPath` when empty; `macroSpecRepoPath` is its single
  reader, so `FindMacroSpecDir` and the slicing inherit it.
- **Macro worktree** follows the task worktree conventions
  (`.tasks/worktrees/<macro key>`, branch named after the macro key, based on
  an up-to-date default branch), gated on `UseWorktrees`; `findMacroBranch`
  already matches a branch by macro key prefix.
- **Additional roadmap projects** is a list of extra key prefixes on the
  project, read-only: they only let a story key attach to a slicing line,
  never trigger a write (taskativ invariant "a declared roadmap project is
  read, never written").
- UI strings stay in French; code, comments and docs in English.

### Product questions

1. **Q1 - Delivery shape.** Recommended: split into child tickets under M-7;
   #426 keeps the coupled chain *specifications repository → macro worktree →
   `realign-macro`*; separate tickets for sprint management, `TargetProjectID`,
   additional key prefixes, and `scenarios` + field map.
2. **Q2 - Sprint management.** Recommended: tracker writes as in taskativ
   (Jira sprints, GitLab iterations; nothing on GitHub), operations create,
   rename, dates, close.
3. **Q3 - Cross-project story creation.** Recommended: allow only target
   projects on the same tracker instance as the macro, where the parent link
   holds; refuse others with an explicit message.
4. **Q4 - `scenarios` source.** Recommended: defer, together with the EPIC
   template field map, to its own ticket.

## Round 2 - answers from the owner (2026-09-24)

Answers given interactively in the session. The owner confirmed the
clarification is satisfactory; the exit condition is met.

### Settled decisions

1. **Q1 / Q7 - Delivery shape: everything in #426, one pull request.**
   *Reverses the round 1 recommendation* (split into child tickets). All the
   in-scope items below ship from `feat/426` in a single pull request, one
   commit per item, in dependency order.
2. **Q2 - Sprint management writes to the tracker**, as taskativ does: Jira
   Agile sprints and GitLab iterations, synchronous calls mirrored locally.
   Nothing on GitHub, which has no sprints; the controls are not offered there.
3. **Q6 - Sprint operations: all four.** Batch creation (name pattern, count,
   start date, duration of 1 to 4 weeks), rename and dates, close, delete.
4. **Q3 - Cross-project story creation: same tracker instance only.**
   `CreateStoryFromMacroTodo` consumes `TargetProjectID` and creates the story
   in the target project when that project is on the same tracker instance as
   the macro, so the parent link holds. Any other target is refused with an
   explicit message naming why; nothing is created.
5. **Q5 - The EPIC template field map is out of scope**: it is specific to one
   Jira integration and is not integrated into Sectile.
6. **Q4 / Q8 - The `scenarios` slicing source is dropped.** *Reverses the Q4
   answer "include"*, which the Q5 answer made impossible (the reader has
   nothing to read without the field map). The owner chose to remove it from
   the code rather than keep a dead constant: `models.MacroTodoFromScenarios`
   (`internal/models/models.go:265-267`) and `'scenarios'` in
   `MacroTodoSource` (`web/src/types/index.ts:125`) are deleted, and every
   comment that lists three repository/tracker sources is updated.

### Settled scope of #426

- Specifications repository: optional second path on the project, falling
  back to `RepoPath`, read through `macroSpecRepoPath` (numbered migration).
- One worktree per macro, in the specifications repository, on the macro's
  branch, based on an up-to-date default branch, gated on `UseWorktrees`.
- `realign-macro`: macro-scoped skill, Spec Kit and OpenSpec variants,
  surgical (add, rename, mark orphans as to be removed; never touch an existing
  entry's body; never delete), writing only in the macro worktree.
- Sprint management on Jira and GitLab: batch create, rename/dates, close,
  delete.
- `TargetProjectID` consumed, same tracker instance only.
- Additional roadmap key prefixes on the project, read-only: a story key from
  a declared project attaches to a slicing line; nothing is ever written to
  that project.
- Removal of the `scenarios` source.

### Out of scope

- EPIC template field map (read, display, edit) and anything built on it.
- A `specify-macro` skill.
- Sprints on GitHub.

### Assumptions carried into the specification

- The round 1 reversible choices stand (skill registration next to
  `refine-macro`, worktree naming after the macro key, settings in project
  options, French UI strings).
- The changelog gets one line per user-visible item under `[Unreleased]`.

### Open questions

None.

## Round 3 - decisions taken during specification (2026-09-24)

Reading the code for the specification surfaced three facts the earlier rounds
did not account for. The owner answered them interactively.

1. **Sectile has no GitLab tracker** (only `github`, `jira` and `local`
   ticketing adapters). Decision: sprint management ships **on Jira only**;
   GitLab iterations go to a follow-up ticket that depends on a GitLab tracker
   adapter.
2. **No agent launch path exists for a macro skill** (the "Raffiner AI" button
   calls a server heuristic). Decision: **both** a button in the macro panel
   that runs `realign-macro` through the desktop app's Run, **and** an MCP tool
   that prepares the macro worktree for a skill invoked by hand.
3. **A story created under a Jira epic gets its parent only locally.**
   Decision: write the epic as the story's parent **on Jira**, in the macro's
   project and in a target project alike.

Left open by the specification (see `specs/426-port-remaining-macro-workflow/spec.md`):
how a slicing line's target project is chosen in the UI (O1), and whether local
sprints already stored on a GitHub project stay visible read-only (O2).
