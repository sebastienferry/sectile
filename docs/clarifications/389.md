# Clarification: #389

## Round 1 — 2026-09-23

The desktop project Deployment panel must offer explicit provider initialization equivalent to the CLI. The server web UI and automatic setup of every provider are out of scope.

Settled by the owner: select a provider, initialize managed skills and MCP together, share initialization logic with the CLI, preserve CLI arguments, report both outcomes, and support rerunning.

Inspected `internal/agent/init.go`, `internal/agent/agent_desktop.go`, `internal/agentconfig/locations.go`, `internal/agentconfig/local.go`, and `desktop/src/main.js`. Replace the skills-only desktop button with provider selection and Initialize; retain framework deployment. The desktop surface is English. Providers without a skill convention report skills as skipped. Initialization stops after MCP failure and reports skills as not run; a skills failure retains the successful MCP result.

Dependencies: paired server, mapped checkout, writable provider configuration, stable agent executable. No open product questions. `.taskflow/config.json` is absent; live Sectile project context confirms GitHub and Spec Kit.
