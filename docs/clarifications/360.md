# Clarification — GitHub #360

Task: `gh-ef5a2777-920f-4744-a7e8-a58a4c257a23-360`
URL: https://github.com/sebastienferry/sectile/issues/360
Branch: `feat/360`
Worktree: `/Users/sferry/Sources/sectile/.tasks/worktrees/#360`
Stage: `new` (Round 2)

## Round 1 — analysis (2026-09-22)

### Restated request and scope

Update the repository root `README.md` and translate all remaining French text to English so the document conforms to the project's English-only documentation policy (`AGENTS.md`). Out of scope: other README files (`web/README.md`, `desktop/README.md`, `docs/README.md`, which are already in English), translating runtime application strings or UI locales in `web/` or `desktop/`, and authoring brand new architectural documentation beyond updating stale references.

### Files read

- `README.md`: line-by-line inspection of French sections (intro tagline at line 8, `## ✨ Fonctionnalités implémentées` at lines 12–85, `## 📚 Documentation Technique Complète` at lines 447–457, `## ⌨️ Raccourcis Clavier` at lines 459–468, and obsolete references like line 649 `Agents à configurer` and line 83 web PTY bar).
- `AGENTS.md`: English-only production policy for documentation, commit messages, and code comments; runtime UI exception policy.
- `docs/README.md`: verified canonical English titles and descriptions for all technical documentation references.
- `web/src/locales/translations.ts`: verified canonical English translations for UI settings, tabs, buttons, and workflow stage names.
- `web/src/components/ProjectModal.tsx`: verified the current agent settings tab layout and confirmation that the multi-agent checkboxes ("Agents à configurer") were previously retired (commit `715e90b`).

### Verified findings

1. **The majority (~85%) of `README.md` is already in English.** Detailed operational sections (Quick start, PostgreSQL migration, Tracker credentials, Releases & SemVer, Remote execution and MCP, Desktop host and companion, Execution modes, MCP naming upgrade) are already authored in English from previous feature deliveries.
2. **Four primary sections remain in French:**
   - **Line 8**: The introductory tagline (`Outil moderne et agentique de gestion des tâches pour développeurs et équipes techniques...`).
   - **Lines 12–85**: `## ✨ Fonctionnalités implémentées` and its subsections (Spec-Driven Design, AI Copilot & configurable engines, sidebar & workflow stages, profile & personalized ergonomics, Kanban board & list view, quick search & action palette).
   - **Lines 447–457**: `## 📚 Documentation Technique Complète` intro and file summary list (for which exact English counterparts already exist in `docs/README.md`).
   - **Lines 459–468**: `## ⌨️ Raccourcis Clavier` table header and action descriptions.
3. **Obsolete and inconsistent statements in `README.md`:**
   - Line 83: Mentions selecting skills in the web PTY terminal bar, whereas the web PTY terminal was retired in favor of agent-owned desktop consoles (`desktop/README.md`).
   - Line 57: References `/pick-issue` instead of canonical `/pickup-issue`.
   - Line 649: Mentions `**Agents à configurer** checkboxes in the project's AI tab`, which was removed in commit `715e90b` in favor of direct active provider configuration and `sectile-agent init --provider <provider>`.
   - Lines 231, 246, 256: Reference French UI button/tab names (`*Connecter votre tracker*`, `*Profil > Trackers*`) within English paragraphs.

### Dependencies and constraints

- Adherence to `AGENTS.md`: "All generated code ... and all documentation (`README.md`, `CHANGELOG.md`, `/docs`, ADRs) must be authored strictly in **English**."
- No database migrations, code modifications, or runtime translation changes are required.
- Standard Markdown formatting, anchor links, and external documentation references must remain functional.

### Reversible technical decisions (taken here)

- **Root `README.md` only**: Target strictly `README.md` in the repository root. `web/README.md`, `desktop/README.md`, and `docs/README.md` are already English.
- **Harmonize documentation links with `docs/README.md`**: Reuse the English descriptions already present in `docs/README.md` for the technical documentation index.
- **Clean up obsolete references**: When translating, update obsolete statements (the removed web PTY bar and removed `Agents à configurer` checkboxes) so the English README is accurate.

### Open product questions

1. **Scope of the update:**
   Should the task strictly be a clean translation to English and alignment of obsolete references (e.g. retiring mentions of the removed web PTY bar and obsolete checkboxes), or should it also add new feature sections for recent capabilities (e.g. personal sealed credentials, desktop changes inspector)?
   *Recommendation: Option A — Clean translation to English and prune obsolete statements to match current functionality, without expanding document scope.*

2. **Language convention for UI labels in the README:**
   Should references to UI elements and workflow stages in the English README use English UI terms (e.g., `Connect your tracker`, `Profile > Trackers`, `To clarify ➔ Specified ➔ In progress ➔ To validate ➔ Done`), or keep French / bilingual terms?
   *Recommendation: Option A — Standard English UI labels matching `web/src/locales/translations.ts` (`en`), since the document is being standardized to English.*

## Round 2 — answers from the owner (2026-09-22)

The owner answered both questions interactively and selected the recommended baseline:

1. **Scope of the update**: Clean translation to English and prune obsolete statements (remove retired web PTY bar reference and removed `Agents à configurer` checkboxes) while keeping the document structure intact.
2. **Language convention for UI labels**: Use standard English UI labels matching the application's English locale (`web/src/locales/translations.ts` `en`), including `Connect your tracker`, `Profile > Trackers`, and `Backlog ➔ To clarify ➔ Specified ➔ In progress ➔ To validate ➔ Done`.

### Settled scope and decisions

1. **Target**: Strictly the root `README.md` file.
2. **Sections to translate**:
   - Line 8: Intro tagline.
   - Lines 12–85: `## ✨ Implemented features` and all bullet points.
   - Lines 447–457: `## 📚 Comprehensive Technical Documentation` section heading, intro text, and bullet point descriptions matching `docs/README.md`.
   - Lines 459–468: `## ⌨️ Keyboard Shortcuts` table headers and action descriptions.
   - Lines 231, 246–247, 256–257: Update French UI references to English (`*Connect your tracker*`, `*Profile > Trackers*`).
3. **Pruned obsolete mentions**:
   - Retire the obsolete mention of selecting skills in the web PTY terminal bar (line 83), noting that skill execution is hosted in the agent-owned desktop console.
   - Fix `/pick-issue` to `/pickup-issue` (line 57).
   - Update line 649 to remove the obsolete mention of the "Agents à configurer" checkboxes, aligning with the current active provider configuration and `sectile-agent init --provider <provider>`.
4. **Zero open product questions remain.**
