# Clarification: GitHub #416

Task: `gh-11a4f59c-b6bd-4747-8ea5-0a10d5b78da4-416`
URL: https://github.com/sebastienferry/sectile/issues/416
Branch: `fix/batch-415-416-417` (batch with #415 and #417)

## Round 1 (2026-09-23)

### Restated request and scope

`web/tests/condensed-card.browser.mjs` must pass again: the mocked `useApp`
has to reach every component the real `TaskCard` renders, not only
`TaskCard.tsx`, and the mocked context has to hold what those children read.
Out of scope: any change to `TaskCard` or its children, and to what the test
asserts about the condensed card.

### Verified findings

- Reproduced from `.tasks/worktrees/batch-415-416-417/web`: `useApp must be
  used within an AppProvider`, then a timeout on the first `waitFor`.
- The fixture rewrites `useApp` only when `id.endsWith('/TaskCard.tsx')`, and
  matches the import with a literal string.
- Children of `TaskCard` that call the real `useApp`:
  - `RemoteRunBadge` reads `activities`, `fetchActivities`, `addToast`;
  - `CopyTaskSkillMenu` reads `skillCommand`, `projects`, `currentProject`;
  - `EpicMarker` (`useEpicColors`) reads `projects`, `currentProject`.
  `PullRequestStateIcon`, `Avatar` and `RunStateGlyph` do not use the context.
- `backlog-batch.browser.mjs` already rewrites every file under
  `/src/components/` with a regular expression that accepts either quote.

### Ambiguities (worst first)

None that changes the acceptance criteria.

### Reversible technical choices (settled from the code)

1. **Rewrite `useApp` in every file under `/src/components/`**, with the same
   regular expression as the backlog batch fixture, so a child added to the
   card later is mocked as well instead of breaking the test again.
2. **Complete `window.ctx`** with `projects: [{ id: 'p' }]`, the matching
   `currentProject`, `skillCommand` returning its default command, an async
   no-op `fetchActivities` and a spied `addToast`. Epic colours stay off for
   this project: the epic bar is covered by the backlog batch test (#415), and
   the condensed card assertions do not depend on it.
3. **No production change and no changelog entry.**

### Critical dependencies

None.

### Product questions

None.

### Outcome

Exit condition met in Round 1: no open product question.
