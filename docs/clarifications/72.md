# Clarification — GitHub #72

## Identity and outcome

- Task: `gh-9dc93da8-0cb4-4fd5-af38-663433d960ff-72`
- Project: `9dc93da8-0cb4-4fd5-af38-663433d960ff` (`sebastienferry/taskflow`)
- Ticket: https://github.com/sebastienferry/taskflow/issues/72
- Branch: `feat/72` (assigned worktree retained)
- Description: "Order by status and execution start time (status : active, queued)"
- No comments or parent identifier were returned by the task interface; `parentType: macro` alone does not identify a parent to read.
- Outcome: clarification complete; the user resolved all three product decisions. Ready for specification.

## Restated request and scope

Order the project's task list by execution state, placing active executions before queued executions, and use execution time to order tasks within each state. The user confirmed the desktop project sidebar as the target; queue scheduling, workflow status changes, and web views are outside scope.

## Verified code and neighboring behavior

- `desktop/src/main.js`: `render()` groups executions by project and task, retaining task insertion order. Projects are alphabetic. Each task's executions are sorted by creation time and ID; the representative is the most recently created running, queued, or preparing execution, otherwise the latest finished execution. The history selector separately uses chronological creation order.
- `cmd/server/agent_desktop.go`: `/desktop/runs` iterates a Go map without sorting. Its `desktopRun` contract exposes `createdAt`, but no `startedAt`. Consequently task row order can vary across refreshes.
- `cmd/server/agent_run.go`: `enqueueRun` records creation time at submission; queue scheduling uses a separate sequence. A run changes from queued to preparing when it obtains a slot.
- `cmd/server/agent.go`: the running desktop record retains the original creation timestamp when the console is launched.
- `desktop/tests/console.ui.cjs`: existing coverage verifies one row per task, execution history, relaunch, selection, and archive behavior; it does not establish cross-task chronological ordering.
- `web/src/components/ListView.tsx`: a distinct web task table defaults to priority sorting and status grouping. It is another possible interpretation of the title.
- `web/src/components/TaskCard.tsx` and `web/src/components/ActivitiesView.tsx`: neighboring web views recognize running and queued/pending activity states.
- `web/src/types/index.ts`, `web/src/context/AppContext.tsx`, and `internal/db/db.go`: web activities have optional start timestamps, are loaded through the activity API, and are ordered by creation time on the server. These are distinct from the desktop run contract.
- `docs/clarifications/56.md` and `openspec/changes/57-clean-up-task-line-in-sidebar/proposal.md`: reviewed for reporting conventions and neighboring desktop scope. The #57 proposal describes separate sidebar layout work; its layout is not assumed implemented here.
- `.taskflow/config.json` and `AGENTS.md`: project identity and workflow confirmed. No `.agents/MEMORY.md` was present.

## Ambiguities and numbered questions, highest impact first

1. **Target surface — resolved:** the user answered "1. desktop." Apply the change to desktop sidebar task rows within each project.
2. **Chronological direction — resolved:** the user accepted the recommendation: active, then queued, then finished; newest first within each group. This is a display rule, not a scheduler change.
3. **Timestamp meaning — resolved:** the user selected actual execution start. Use actual start for started runs and submission time for queued runs. Actual start requires extending the desktop run contract; using creation time everywhere would produce different visible ordering after queue delays.

All questions are resolved by explicit user answers. No further product clarification is required.

## Settled boundaries and proposed reversible defaults

- Clarification only: no production code, specification, commits, or PR creation at this stage.
- Preserve the assigned worktree, branch, and pre-existing skill-file edits.
- Desktop scope is confirmed. Retain alphabetic project order, one row per task, archive filtering, history order, and current selection when rows reorder.
- Treat running/preparing as active, queued as the next group, and completed/failed/canceled as the final group. For multiple executions on a task, prefer the highest-ranked state before choosing the newest execution by effective timestamp in that state; otherwise a newer queued execution can hide a running execution. This is a reversible convention chosen for consistent row status and ordering.
- Use a deterministic task/run identity tie-breaker. Missing or invalid start timestamps fall back to creation time; absent usable times sort after known times within the state group.
- Keep ordering local to presentation and do not alter execution scheduling or tracker task statuses.

## Critical dependencies and next action

- No unresolved product decisions remain.
- The local agent must capture and expose actual start time when a run becomes running, retaining compatibility with runs without that field. Preparing runs use submission time until their actual start exists. No database migration or external team dependency has been identified.
- Sidebar layout work in #57 overlaps `desktop/src/main.js`; reconcile current code during specification/implementation without absorbing unrelated layout changes.
- Next workflow stage: specification using the project's OpenSpec framework. No tests were run because clarification only inspected code and wrote this report.
