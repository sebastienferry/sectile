# Clarification — GitHub #48

## Current standalone verification — 2026-09-12

This section supersedes the historical transition blockers below. Live `taskflow_get_task` now resolves the exact task ID `gh-9dc93da8-0cb4-4fd5-af38-663433d960ff-48` to the intended GitHub issue, with the unchanged description, no comments returned, and status `to_clarify`. Live project context confirms project `9dc93da8-0cb4-4fd5-af38-663433d960ff`, name `taskflow`, repository `git@github.com:sebastienferry/taskflow.git`, and OpenSpec. The local configuration discrepancy remains, but the exact MCP task identity is now verified. The earlier parent check found no parent; the current MCP response supplies no parent context.

Newly added tasks should use lowercase `new`, displayed as `#new`, throughout the add form and creation result, including clones. Historical migration, changes to other workflow stages, global label normalization, and provider taxonomy changes remain outside scope.

Re-read `internal/db/db.go`, `internal/db/labels_test.go`, `internal/db/trackerops.go`, `internal/runner/runner.go`, `web/src/components/ListView.tsx`, `web/src/components/TaskCard.tsx`, and additionally `web/src/components/QuickAddModal.tsx`. The add modal initializes its labels to `['New']` each time it opens and submits that array; it must be included alongside the backend creation and cloning defaults. Existing workflow replacement preserves custom labels and accepts legacy casing. Keep status behavior, hash-prefix conventions, and Linear's separate provider mapping unchanged.

Ambiguities, in priority order, are resolved as explicit reversible assumptions: creation-only rather than historical migration; include cloning because it delegates to creation; include the add-form default rather than only changing persisted data. No essential product decisions or unavailable dependencies remain. Questions: none required.

The clarification is ready for specification. Use the assigned branch `48-default-label-when-adding-a-task`. No production code or specification was changed and no tests were run for this clarification. Record completion through the standalone MCP transition using the exact task ID; the historical reports below are retained for provenance.

## Request and scope
Newly added tasks must show the default workflow label as `#new` instead of `#New`. This is a casing correction for task creation, including cloning; existing-task migration, other workflow stages, and unrelated label changes are outside scope.

## Evidence
- GitHub issue #48, “Default label when adding a task”, contains the supplied description and no comments (`gh issue view 48 --repo sebastienferry/taskflow --json title,body,comments`).
- `internal/db/db.go`: `CreateTask` sets the workflow label to `New`; `CloneTask` also sets `New` before delegating to `CreateTask`. `SetWorkflowLabel` removes existing workflow variants case-insensitively but preserves the supplied target casing and hash-prefix convention. Non-workflow labels are retained.
- `internal/db/labels_test.go`: existing tests cover workflow replacement and stale-label handling.
- `internal/db/trackerops.go`: GitHub status synchronization already uses lowercase workflow labels, including `#new`.
- `internal/runner/runner.go`: Linear has a separate provider label mapping (`new` to `New`). Preserve provider compatibility rather than treating this ticket as a remote taxonomy migration.
- `web/src/components/ListView.tsx`: the new-stage label is already `#new`.
- `web/src/components/TaskCard.tsx`: inspected the compact label rendering.
- `.taskflow/config.json`: checked project context. It currently specifies a local tracker and an empty GitHub repository, contrary to the managed invocation. The explicit managed contract and supplied GitHub repository identify this ticket; no configuration edits or workflow transitions are needed.

## Ambiguities and resolved assumptions
1. New tasks only or historical normalization: assume new tasks only, because the request explicitly concerns adding a task. No data migration.
2. Include cloning: yes, as a clone creates a new task through the same creation function. Preserve its existing status and custom-label semantics.
3. Display casing or global label canonicalization: correct the default creation label casing while retaining existing prefix conventions and tracker-specific mappings. Do not lowercase arbitrary user labels or change the general workflow helper globally.

These are reversible scope choices based on the code, not answers attributed to the user.

## Critical dependencies
No unavailable dependency or essential product decision blocks specification. Implementation should verify the returned and persisted creation label, clone behavior, and preservation of custom labels. Remote tracker taxonomy changes are not required.

## Questions
None required; the request and existing code resolve the scope sufficiently.

## Handoff
Specification can proceed with the assumptions above. No production code or executable specification was written during clarification. The assigned branch `48-default-label-when-adding-a-task` is retained as required by the managed invocation. TaskFlow owns stage transitions and tracker synchronization.

## Standalone review — 2026-09-12

The preceding report was already present when this invocation began; its managed-run statements describe that earlier invocation. This invocation supplied no managed result-file contract.

Re-read `internal/db/db.go`, `internal/db/labels_test.go`, `internal/db/trackerops.go`, `internal/runner/runner.go`, `web/src/components/ListView.tsx`, and `web/src/components/TaskCard.tsx`. The code still supports the recorded scope. In particular, the card adds the visible `#` itself, so lowercase `new` can satisfy the display requirement without changing the stored prefix convention. No production code or specification was changed.

Current task and project-context reads through TaskFlow were rejected. The earlier report's ticket description and no-comments claim could therefore not be independently reverified, including any subsequent scope changes or parent context. No alternative tracker access was attempted after rejection. The configuration discrepancy remains unresolved.

The local code review is complete, but current ticket verification and the standalone `clarified` transition remain pending. Do not treat this review as confirmation that the stage was updated. Once task access is available, verify the current description, comments, and any parent context before recording the completed stage through the local handler.

## Verified standalone clarification — 2026-09-12

This review supersedes the preceding review's pending ticket-verification statement. GitHub issue `sebastienferry/taskflow#48` was successfully read: title “Default label when adding a task”; description “Currently the default label is #New, it should be #new (lowercap)”; no comments. The GitHub parent endpoint reports “No parent issue found”.

Rechecked `internal/db/db.go` (`SetWorkflowLabel`, creation and cloning defaults), `internal/db/labels_test.go`, `internal/db/trackerops.go`, `internal/runner/runner.go`, and label rendering in `web/src/components/ListView.tsx` and `web/src/components/TaskCard.tsx`. The existing scope and reversible assumptions above remain supported. Creation and cloning supply `New`; the helper preserves supplied casing; task cards add the visible hash prefix. Use lowercase `new` for the creation default, preserving custom labels, prefix conventions, status semantics, and provider-specific mappings. No historical migration or global label canonicalization is required. No product questions or implementation dependencies remain open.

The assigned branch remains `48-default-label-when-adding-a-task`. No production code or specification was changed, and no tests were run for this documentation-only clarification.

### Transition blocker

The standalone handler's read endpoint `http://localhost:8090/api/tasks/%2348` returned task `gh-48`, project `default`, linked to `https://github.com/sebastienferry/fretzee-studio/pull/48`, titled “feat(editor): add color indicator badge and font colorization for C mode (#47)”. This is not the verified TaskFlow issue. Its existing `clarified` status does not establish the status of `sebastienferry/taskflow#48`.

No transition POST was sent because it would target the wrong task. The clarification report is complete; workflow synchronization remains pending until the local TaskFlow project/task mapping resolves to `sebastienferry/taskflow#48`. The local config declares a local tracker with an empty repository, while AGENTS.md and remote-config identify the TaskFlow GitHub repository; these configuration files were preserved without edits.

### Latest MCP verification — 2026-09-12

The current standalone invocation successfully called both `taskflow_get_task` and `taskflow_get_project_context`. The task still resolves to the unrelated Fretzee Studio pull request above, and project context explicitly returns project `default`, name `Fretzee Studio`, and repository `git@github.com:sebastienferry/fretzee-studio.git`. Thus MCP availability is restored, but task identity remains incorrect. No transition or comment was submitted.

Read-only GitHub verification again confirmed the intended TaskFlow issue title and description, zero comments, and no parent issue. Local code inspection reconfirmed the creation and cloning defaults, workflow helper behavior, neighboring tracker mappings, label rendering, and existing label tests. The settled scope and assumptions remain unchanged; no essential product questions remain. Only this clarification report was updated. Specification and production changes were not started.
