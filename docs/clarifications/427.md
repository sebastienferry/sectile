# Clarification: #427 - Open ID Connect endpoints

## Round 1 - initial framing (2026-09-26)

Status: awaiting the owner's scope decisions; the clarified exit condition is not met.

### Request

Clarify the endpoint-related OpenID Connect capability requested by #427, whose title is currently its only requirement. Implementation and technical specification are outside this round; no new authentication role or endpoint contract is assumed.

### Source and checkout

- Task: https://github.com/sebastienferry/sectile/issues/427
- Task ID: `gh-11a4f59c-b6bd-4747-8ea5-0a10d5b78da4-427`.
- Project ID: `11a4f59c-b6bd-4747-8ea5-0a10d5b78da4`.
- Sectile returned an empty description and no comments. No parent key was supplied, despite the generic `parentType: macro` field.
- No earlier clarification report exists in this checkout.
- The supplied worktree is clean and detached at `18f021c1`; no assigned branch was returned by Sectile. Keep this checkout without creating or switching branches.
- `.taskflow/config.json` is absent; project identity and workflow references were read through Sectile MCP.

### Existing behavior and evidence

- `internal/auth/oidc.go` implements Sectile as a client of an external identity provider. It reads issuer, client ID, secret and redirect URL from environment variables, discovers authorization, token and UserInfo URLs at startup, and rejects missing endpoints or an issuer mismatch.
- The same file implements authorization code exchange with PKCE and reads identity from the provider's UserInfo endpoint.
- `internal/handlers/auth.go` starts login, handles the callback, creates browser sessions and revokes the local session at logout. Local e-mail sign-in is disabled when a provider is configured.
- `cmd/server/main.go` registers `/auth/login`, `/auth/callback` and `/auth/logout`.
- `README.md`, "Signing in and pairing a workstation", documents the current configuration and mandatory sign-in behavior.
- `docs/adrs/0008-web-sign-in-through-openid-connect.md` establishes the external-provider design. Its historical implicit-user fallback is superseded by the local sign-in behavior visible in the current handler and README.
- `AGENTS.md` and `.agents/MEMORY.md` were consulted for workflow and project constraints.

These observations describe existing code, not a protocol compliance audit or a verified external-provider deployment.

### Ambiguities, highest impact first

1. Does "endpoints" mean consuming an external provider for Sectile sign-in, publishing identity-provider endpoints for other applications, or configuring/exposing the provider URLs? These lead to fundamentally different contracts and code.
2. What concrete user journey currently fails or is missing, on which surface and with which provider/client? Existing browser sign-in may already cover part of the intended request.

### Proposed defaults, pending scope confirmation

- For an improvement to existing sign-in, extend the current provider client and session handlers. Recommend keeping Sectile's existing client role unless another application explicitly needs Sectile to act as its identity provider.
- Preserve current account identity, roles and workstation credentials unless the confirmed use case requires a change.
- Reuse project configuration and documentation conventions. Do not choose new endpoint paths, migrations or dependencies before the scope is known.

### Critical dependencies

- An owner-supplied example of the intended provider/client and successful user journey.
- For external-provider integration: a reachable provider, application registration and accepted callback URL to validate the eventual flow; no secrets are needed for clarification.
- If Sectile must become an identity provider: a separate architecture decision and an explicit consumer contract before specification.
- No migration or new library is justified by the title alone.

### Questions for the owner

1. Which outcome is intended: improve Sectile sign-in through an external provider (recommended if this is an SSO request), publish Sectile identity endpoints for other applications, or configure/display provider endpoint URLs?
2. Give one acceptance example: who uses which surface (web, desktop, MCP/API or another application), with which provider/client, what happens today, and what should happen after this change?

### Workflow outcome

Round 1 records the available evidence and two open product questions. No production code, specification or changelog change is made. The task remains `to_clarify`; no `clarified` transition is valid until the answers settle the scope and the owner confirms the clarification.
